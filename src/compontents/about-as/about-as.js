import Header from "../header/header";
import AboutAsDetails from "./about-as-details";

const AboutAs = (props) => {
    return(<div>
        <Header/>
        <AboutAsDetails/>
    </div>
    )
}

export default AboutAs